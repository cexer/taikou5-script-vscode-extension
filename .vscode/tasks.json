{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "打包插件成VSIX",
            "type": "shell",
            "command": "npx vsce package --no-dependencies",
            "group": "build",
            "problemMatcher": [],
            "detail": "Generate .vsix package using vsce",
            "options": {
                "shell": {
                    "executable": "cmd.exe",
                    "args": [
                        "/d",
                        "/c"
                    ]
                }
            }
        }
    ]
}